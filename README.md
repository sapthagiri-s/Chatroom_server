# Chatroom_server
Chatroom Server is a multi-client TCP chat server. It handles user registration and authentication, manages multiple client connections concurrently using threads or processes, tracks online/offline user status, broadcasts messages, handles client disconnects gracefully, and demonstrates Linux socket programming, IPC, and concurrency concepts.
